/** Legacy color export kept for compatibility. Use `constants/theme` instead. */
import { colors } from "./theme";

export default {
  light: {
    text: colors.text,
    background: colors.appBg,
    tint: colors.primary,
    tabIconDefault: colors.textHint,
    tabIconSelected: colors.primary,
  },
};
